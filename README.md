## Источник проекта

Стажировка в рамках обучения в Университете искусственного интеллекта (УИИ)

**Суть проекта:** Построить сервис для контроля действий кассира во время валютно-обменных операций

**Моя зона ответственности:** CV-ядро — классификация действий кассира

**Ключевое ограничение:** Малый датасет (продукт разрабатывался для банка, согласование большего объёма данных затруднено)

---

## Предыстория

В рамках стажировки тимлид выделил четыре направления: разметка данных, process compliance, frontend/backend и CV-core. Я входил в группу CV-core. Длительность стажировки — три месяца (март–май 2026).

На момент публикации репозитория (26 апреля) датасет от группы разметки ещё не был получен. Поэтому мы (CV-core) самостоятельно нарезали кропы рук и классифицировали их. Исходное видео длилось 9 минут, мы разметили 1 минуту — около 3600 изображений рук. Исходное видео не может быть опубликовано по NDA.

### Структура данных
```
other/
├── img_1
├── img_2
└── img_n

money/
├── img_1
├── img_2
└── img_n

empty/
├── img_1
├── img_2
└── img_n
```


### Планировавшаяся, но нереализованная детализация классов

Изначально предполагалось большее количество классов:
- различные валюты: `money_byn`, `money_usd`, `money_rub`
- различные действия other: `other_doc`, `other_print`, `other_money_machine`
- `empty` как нейтральное состояние (бездействие)

Однако из-за отсутствия размеченных данных от группы разметки эксперименты временно велись только на трёх агрегированных классах.

---

## Данные после фильтрации MediaPipe

С помощью MediaPipe Hand Landmarker я выделил руки из кропов. Удалось детектировать 388 изображений. Распределение по классам:

| Класс | Количество кропов |
|-------|-------------------|
| other | 130 |
| money | 113 |
| empty | 145 |

**Примечание.** MediaPipe был требованием тимлида — у каждого участника группы была «своя» модель для экспериментов. Из-за этого ограничения использовать большее количество детектированных кропов не представлялось возможным.

---

## Задача эксперимента

Сравнить три подхода к классификации жестов кассира на ограниченном датасете:

1. **ModelMaker** (встроенный классификатор MediaPipe)
2. **CNN** (свёрточная сеть на изображениях)
3. **CNN + координаты** (гибридная модель: изображения + 21 ключевая точка руки)

---

## Функции обработки данных

| Функция | Назначение |
|---------|------------|
| `collect_coords` | Извлечение координат 21 точки руки из кропов через MediaPipe, нормализация вычитанием запястья |
| `euclid_dist` | Вычисление евклидовых расстояний от кончиков пальцев до основания ладони |
| `cosine_dist` | Вычисление косинусных расстояний между векторами пальцев |
| `angle_features` | Вычисление углов между векторами пальцев (через arccos) |

---

## Подход 1: ModelMaker (MediaPipe)

**Функция:** `def model_maker_model()`

**Что это:** Встроенный классификатор MediaPipe, который заменяет последние слои модели детекции и дообучается на предоставленных данных. Требует наличия класса `none` для всего, что модель не может однозначно определить. На вход фактически принимаются изображения, внутри модели извлекается 21 точка (63 признака).

**Результат:**

| Test loss | Test accuracy |
|-----------|---------------|
| 0.364 | 63.0% |

Несмотря на невысокий loss (модель уверена в своих предсказаниях), точность оставляет желать лучшего. Это может объясняться тем, что ModelMaker обучается только на 63 признаках (координатах точек) и не использует информацию из самих изображений. Для дообучения «верхушки» модели MediaPipe, вероятно, требуется больше данных или более выразительные признаки.

---

## Подход 2: CNN (только изображения)

**Функции:** `def build_model_conv2D()`, `def model_conv2D_eval(model, data)`

**Архитектура:** Простая свёрточная сеть. Принимает изображения, многократно пропускает через свёртку и пулинг, затем flatten + dense слои.

**Результат:**

| Test loss | Test accuracy |
|-----------|---------------|
| 0.913 | 83.3% |

### Confusion matrix

| Реальный класс | Предсказано | Расшифровка |
|----------------|-------------|-------------|
| **other** | [23, 3, 0] | 23 угадал, 3 как empty |
| **empty** | [2, 25, 2] | 25 угадал, 2 как other, 2 как money |
| **money** | [0, 6, 17] | 17 угадал, 6 как empty |

**Наблюдения:** Модель демонстрирует хорошую точность, но неуверенна (высокий loss). Путаница возникает между классами `empty` и `money`.

> Были попытки аугментировать датасет после детекции, но ощутимого улучшения они не дали (код и точные результаты утеряны).

---

## Подход 3: CNN + координаты (гибридная модель)

**Функция:** `def build_conv2d_and_coords_model()`, `def eval_conv2d_and_coords_model(model,data)`

**Архитектура:** Модель в функциональном API Keras с двумя входами:
- изображение (224×224) → свёрточная ветка
- координаты (63 признака) → полносвязная ветка

Затем ветки объединяются через `Concatenate`, и идёт общий классификатор.

Эксперимент проводился в три этапа.

---

### Этап 3.1: «Голые» координаты + изображения

На вход подаются изображения и сырые координаты 21 точки (63 числа) без дополнительной обработки.

**Результат:**

| Test loss | Test accuracy |
|-----------|---------------|
| 0.511 | 79.5% |

**Confusion matrix:**

| Реальный класс | Предсказано | Расшифровка |
|----------------|-------------|-------------|
| **other** | [24, 2, 0] | 24 угадал, 2 как empty |
| **empty** | [7, 20, 2] | 20 угадал, 7 как other, 2 как money |
| **money** | [0, 5, 18] | 18 угадал, 5 как empty |

Модель стала заметно увереннее по сравнению с чистой CNN.

---

### Этап 3.2: Добавление расстояний (евклидовых и косинусных)
<img width="1024" height="357" alt="image" src="https://github.com/user-attachments/assets/30d1c776-eba3-4739-a770-c0f809235400" />

**Добавленные признаки:**
- Евклидовы расстояния (`euclid_dist`) между кончиками пальцев (точки 4, 8, 12, 16, 20) и основанием ладони (точка 0)
- Косинусные расстояния (`cosine_dist`) между направлениями пальцев (каждый палец — вектор от основания к кончику; расстояния считаются попарно)

Размерность координатной части выросла с **63 до 78**.

**Результат:**

| Test loss | Test accuracy |
|-----------|---------------|
| 0.396 | 84.6% |

**Confusion matrix:**

| Реальный класс | Предсказано | Расшифровка |
|----------------|-------------|-------------|
| **other** | [21, 3, 2] | 21 угадал, 3 как empty, 2 как money |
| **empty** | [1, 23, 5] | 23 угадал, 1 как other, 5 как money |
| **money** | [0, 1, 22] | 22 угадал, 1 как empty |

Модель стала увереннее, точность выросла. Следы переобучения сохранились, но стали менее выраженными.

---

### Этап 3.3: Добавление углов между пальцами

**Добавленные признаки:** углы между векторами пальцев (`angle_features`), вычисляемые через arccos от косинусного сходства.

Размерность координатной части выросла до **88**.

**Результат:**

| Test loss | Test accuracy |
|-----------|---------------|
| 0.478 | **87.2%** |

**Confusion matrix:**

| Реальный класс | Предсказано | Расшифровка |
|----------------|-------------|-------------|
| **other** | [23, 3, 0] | 23 угадал, 3 как empty |
| **empty** | [0, 27, 2] | 27 угадал, 2 как money |
| **money** | [0, 5, 18] | 18 угадал, 5 как empty |

**Ключевое улучшение:** класс `empty` перестал путаться с `other` (0 ошибок), что заметно повышает практическую ценность модели.

---

## Сравнение моделей

| Модель | Test loss | Test accuracy | Inference time |
|--------|-----------|---------------|-----------|
| ModelMaker (MediaPipe) | 0.364 | 63.0% | 0.002s
| CNN (только изображения) | 0.913 | 83.3% | 1s
| CNN + координаты (63) | 0.511 | 79.5% | 1.2s
| CNN + координаты + евклид + косинусы (78) | 0.396 | 84.6% |1.5s
| **CNN + координаты + евклид + косинусы + углы (88)** | 0.478 | **87.2%** |1.79s

### Этап 4: Классические ML модели (XGBoost / LightGBM)

Спустя некоторое время после завершения DL-экспериментов я вернулся к задаче с другим подходом. Изначально проект был сфокусирован на свёрточных и гибридных нейросетях, однако после анализа структуры данных стало очевидно, что задача опирается не на визуальные паттерны, а на уже извлечённые геометрические признаки руки.

Так как в датасете уже присутствует явная табличная структура (координаты 21 точки руки + инженерные признаки: расстояния, углы и косинусные меры), было принято решение протестировать классические алгоритмы машинного обучения, работающие с табличными данными без необходимости сложной архитектуры.

Использовались два градиентных бустинга:

- XGBoost  
- LightGBM  

Обе модели обучались на тех же признаках, что использовались в гибридной нейросети (88 engineered features).

---

**Результаты**

| Модель     | Accuracy | Inference time |
|------------|----------|----------------|
| XGBoost    | 0.7949   | 0.0019 s       |
| LightGBM   | 0.8205   | 0.0021 s       |

---

**Наблюдения**

- LightGBM показал лучшую точность среди классических моделей, приблизившись к результатам нейросетевого подхода.  
- Оба бустинга оказались крайне быстрыми на inference (порядка 2 мс), что делает их значительно более лёгкими в эксплуатации по сравнению с CNN и гибридной моделью.  
- Результаты подтвердили ключевую гипотезу: при наличии качественно инженерированных признаков (геометрия руки) классические методы машинного обучения способны конкурировать с глубокими нейросетями.  

---

**Вывод по подходу**

Данный эксперимент был проведён уже после завершения основной части проекта. Его цель — проверить, насколько сложные нейросетевые архитектуры действительно необходимы для задачи, где структура данных уже явно выражена.

Результаты показали, что значительная часть качества может быть достигнута без использования глубокого обучения, за счёт правильно подобранного feature engineering и применения градиентного бустинга.

---

## Общий вывод

Гибридная модель (изображения + координаты + геометрические признаки) показывает лучший результат по качеству — **87.2% accuracy**, однако достигается это ценой увеличения вычислительной сложности и времени инференса (1.79s против 0.002s у легких моделей).

По сравнению с базовой CNN прирост составляет **+3.9% accuracy**, что подтверждает эффективность добавления структурированных геометрических признаков к визуальному представлению.

Добавление инженерных признаков даёт измеримое улучшение качества:

- расстояния (евклид + косинус) → +5.1% к accuracy относительно “сырых” координат  
- углы → дополнительное улучшение до итогового результата (87.2%), несмотря на небольшое ухудшение loss  

Отдельно стоит отметить ключевой результат этапа 3.3: класс *empty* полностью перестал путаться с *other* (0 ошибок). Это критически важно для практического применения модели, так как снижает вероятность ложных срабатываний в сценариях “отсутствия действия”, что особенно важно в production-системах контроля кассира.

Основное оставшееся слабое место модели — путаница между *money* и *empty* (5 ошибок). Это объясняется тем, что часть движений при извлечении или удержании денег и “пустой руки” имеет схожую кинематическую структуру, что приводит к перекрытию классов в feature space.

---

### Дополнительный эксперимент с классическими ML моделями

После завершения основной DL-части проекта был проведён дополнительный эксперимент с классическими алгоритмами машинного обучения на тех же инженерных признаках (88 features).

Использовались модели:

- XGBoost  
- LightGBM  

Результаты показали:

- XGBoost: 79.49% accuracy, 0.0019s inference  
- LightGBM: 82.05% accuracy, 0.0021s inference  

Несмотря на более низкую точность по сравнению с гибридной нейросетью, бустинговые модели продемонстрировали крайне низкое время инференса и высокую эффективность на табличных признаках.

---

### Итоговая интерпретация

Анализ всех подходов показывает, что задача классификации жестов кассира в данном проекте в значительной степени решается через **структурированные геометрические признаки**, а не через сложные визуальные представления.

- Deep Learning (CNN / hybrid) даёт максимальную точность, но требует больше вычислений  
- Gradient Boosting показывает близкое качество при минимальной сложности  
- простые модели оказываются конкурентоспособными благодаря сильной инженерии признаков  

Таким образом, ключевым фактором качества в данной задаче является не архитектура модели, а качество и выразительность признакового пространства.

---

## Идеи, которые не были реализованы, но выглядят перспективно

- «Раскрытие» руки — матрица попарных расстояний между кончиками пальцев, их среднее и разброс
- Нормализация масштаба: деление всех расстояний на «размер руки» (например, расстояние между крайними точками или длина ладони), чтобы скомпенсировать влияние расстояния до камеры

---

## Заключение

Эксперимент я считаю полезным. На очень малом датасете (388 кропов) мне удалось достичь точности **87.2%**. Добавление генерируемых геометрических признаков позволило частично «подтереть» следы переобучения и значительно улучшить разделение классов.

Дальнейшие эксперименты с признаками я прекратил, так как они превратились в борьбу за доли процента — разумнее увеличивать датасет, а не множить признаки.

*Если этот эксперимент окажется кому-то полезен — значит, я провёл его не зря.*

---

## Воспроизводимость

Все эксперименты проведены с фиксированным `random_state=42` при разбиении данных. Это гарантирует воспроизводимость результатов.

```python
# Разбиение train/test/val
X_temp, X_test, y_temp, y_test = train_test_split(..., random_state=42)
X_train, X_val, y_train, y_val = train_test_split(..., random_state=42)
